# Laravel Simple Task Tracker Project

## Installation

First clone this repository, install the dependencies

```
git clone https://github.com/jessicamichel93/simple-task-tracker.git
composer install

```

Then run the following command to start server.

```
php artisan serve
```
